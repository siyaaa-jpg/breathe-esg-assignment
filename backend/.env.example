# Local dev — copy to .env (which is gitignored) and edit.
DJANGO_SETTINGS_MODULE=config.settings.dev
DJANGO_SECRET_KEY=replace-with-something-random-for-prod
DJANGO_LOG_LEVEL=INFO
# DATABASE_URL only needed in prod; dev uses SQLite.
# DATABASE_URL=postgres://user:pass@host:5432/dbname
